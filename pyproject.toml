[project]
name = "janhke_emde"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = ["matplotlib~=3.9"]

[project.scripts]
hello = "janhke_emde:hello"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
